"""Intentional PhantomGuard TUI demo fixture — never install this package."""

# This name is deliberately unique so `scan staged` can demonstrate a real
# PyPI 404 and PhantomGuard's blocking verdict.
import phantomguard_tui_probe_20260717_z9x7k3
import matlobplot
