# Archived Python prototype

This directory preserves the original Python implementation that predated the Go release. It is retained for historical comparison only and is not part of the PhantomGuard build, test, hook, Docker image, or CI workflow.

The supported implementation is the Go module at the repository root.
